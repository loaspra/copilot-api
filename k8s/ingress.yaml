apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: copilot-api
  namespace: default
spec:
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: copilot-api
                port:
                  number: 4141
